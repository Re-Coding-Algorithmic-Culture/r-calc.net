---
layout: layouts/base
---

<!--
	<div id="noscript">Unfortunately this page requires <span style="color:aquamarine">Javascript</span> to be enabled. You won't be able to see much going on here otherwise...</div>
-->

<div id="buckets">
	{%- for entry in collections.blob %}
		<div class="couch icon {{ entry.data.icon }}" onmousedown="startDrag(event);">
			<a href="{{ entry.url | url }}">{{ entry.data.title }}</a>
		</div>
	{%- endfor %}
</div>

<div id="index-background"></div>

<main>
	
	{{ content | safe }}
	
</main>